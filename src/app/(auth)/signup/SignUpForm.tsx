import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Sign Up"
};

export default function SignUpForm() {
    return (
        <div className="flex h-full max-h-[32rem] w-full max-w-[32rem] rounded-2xl outline-hidden shadow-2xl bg-card">
            Form here
        </div>
    );
}